import 'package:flutter/material.dart';

const primaryTitle =
    TextStyle(fontWeight: FontWeight.bold, color: Colors.white, fontSize: 45.0);
const description = TextStyle(fontSize: 20.0, color: Colors.white);
const secondaryTitle = TextStyle(
    color: Colors.yellow, fontWeight: FontWeight.bold, fontSize: 45.0);
const buttonShape = RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(30.0)));
